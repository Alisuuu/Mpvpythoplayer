```
pkg update -y && pkg upgrade -y && pkg install -y python mpv curl ffmpeg git && termux-setup-storage && export LD_LIBRARY_PATH=/data/data/com.termux/files/usr/lib && curl -L https://raw.githubusercontent.com/Alisuuu/Mpvpythoplayer/main/app.py -o app.py && pip install python-mpv && python app.py
```
